{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "bootstrap": "lerna bootstrap --no-ci",
    "ls": "lerna ls",
    "pub": "lerna publish",
    "build": "lerna run build && node build/index",
    "test": "lerna run test",
    "test-ci": "lerna run test-ci",
    "test-cov": "lerna run test-cov"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "babel-eslint": "^10.0.3",
    "eslint": "^6.5.1",
    "eslint-config-brcjs": "^1.1.1"
  }
}
