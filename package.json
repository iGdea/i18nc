{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "bootstrap": "lerna bootstrap",
    "ls": "lerna ls",
    "pub": "lerna publish",
    "build": "lerna run build && node build/index",
    "test": "lerna run test",
    "test-travis": "lerna run test-travis",
    "test-cov": "lerna run test-cov"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "eslint": "^5.5.0",
    "eslint-config-brcjs": "^1.0.0"
  }
}
